# Payment-Processing-System
Simple payment backend built with Spring Boot. Users can register, add money, send money, and check transactions. Transfers are handled safely and duplicate requests are avoided.
